def solution(inputs):
    result = ""
    return result


if __name__ == "__main__":
    # Assume first line is remaining number of lines in input
    num_lines = int(input())
    # Read the rest of the lines into array
    inputs = [input() for _ in range(num_lines)]
    print(solution(inputs))